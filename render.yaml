services:
  - type: web
    name: moltbook-agent
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerCommand: ./start_render.sh
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: GROQ_API_KEY
        sync: false
